O nosso schema do Drizzle já possui a estrutura necessária para autenticação (users, sessions, accounts com a coluna 'password', clinics e user_to_clinics). Precisamos configurar o login por e-mail e senha no Better Auth e criar um seed inicial para testes. Siga estes passos:

1. Localize o arquivo de configuração do Better Auth (geralmente `auth.ts` ou `lib/auth.ts`). Adicione a configuração do provedor de credenciais:
   - Ative `emailAndPassword: { enabled: true }` dentro dos plugins/providers.

2. Verifique se o pacote `bcryptjs` e `@types/bcryptjs` estão instalados. Se não estiverem, instale-os. Vamos usá-lo para gerar o hash da senha no nosso script de seed, garantindo compatibilidade com o padrão do Better Auth.

3. Crie um arquivo de seed em `src/db/seed.ts` (ou no diretório correspondente ao banco de dados) que realize as seguintes operações usando Drizzle:
   - Conecte-se ao banco de dados.
   - Crie uma Clínica chamada "Clínica Padrão".
   - Crie um Usuário com nome "Admin", e-mail "admin@doctorschedule.com".
   - Gere o hash da senha "senha123" usando `bcryptjs.hashSync("senha123", 10)`.
   - Crie um Registro na tabela `accounts`, vinculando ao usuário criado, com `providerId: "credential"`, `accountId: "admin@doctorschedule.com"` e salvando a senha hasheada na coluna `password`.
   - Crie o vínculo na tabela `userToClinicsTable` relacionando o Usuário à Clínica.
   - Adicione logs no terminal indicando sucesso e exibindo o e-mail e a senha criados.

4. Adicione um script no `package.json` chamado `"db:seed": "tsx src/db/seed.ts"` (ajuste o caminho de acordo com a estrutura do projeto).

5. Execute a formatação no código gerado. Não execute o seed, apenas crie os arquivos e me avise quando terminar.

   siga sempre as regras .claude/rules
